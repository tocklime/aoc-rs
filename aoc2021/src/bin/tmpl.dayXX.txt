use std::str::FromStr;

use aoc_harness::*;

aoc_main!(2021 day X, generator lines::<X>, part1 [p1], example part1 EG => 1);

const EG: &str = "";

#[derive(Debug)]
struct X {}

impl FromStr for X {
    type Err = String;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        todo!()
    }
}
fn p1(input: &[X]) -> usize {
    dbg!(input);
    input.len()
}

fn p1a(input: &str) -> usize {
    input.len()
}
