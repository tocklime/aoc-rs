use std::str::FromStr;

use aoc_harness::*;

aoc_main!(2021 day 3, generator lines::<X>, [p1], [p2]);

#[derive(Debug)]
struct X {}

impl FromStr for X {
    type Err = String;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        todo!()
    }
}
fn p1(input: &[X]) -> usize {
    dbg!(input);
    input.len()
}

fn p2(input: &[X]) -> usize {
    input.len()
}