name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: Swatinem/rust-cache@v1
    - name: Build
      run: cargo build --release
    - name: Run tests
      run: cargo test --bin aoc_2019 --bin aoc_2021 --lib --release
    - name: Check clippy
      run: cargo clippy
    - name: Run for score (2019)
      run: cargo run --bin aoc_2019 --release
    - name: Run for score (2021)
      run: cargo run --bin aoc_2021 --release
    - name: Check formatting
      run: cargo fmt --all -- --check

